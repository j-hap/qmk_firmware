#define NO_ACTION_ONESHOT
#define SPLIT_WATCHDOG_ENABLE // otherwise the keyboard is not recognized on a cold boot

#define TAPPING_TERM 200 // default 200ms for a press to become a hold
#define PERMISSIVE_HOLD  // when mod-tap key is pressed an another is pressed and released, it counts as hold, even when the tapping term is not exceeded

#define MASTER_LEFT
